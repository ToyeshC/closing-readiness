TOYESH → EMMA HANDOFF — 2026-05-13 (Day 4)
==============================================

HEADLINE: Toyesh added financial ratios (DSO/DPO/working capital), CIT absolute threshold,
and Exact Online API scaffold. Your app needs to wire guided-diagnosis Claude mode,
expose ratios to the frontend, and add 3 missing check cards.

NOTE: You missed the Day 3 handoff as well. Read it first for context on the 3 new
check IDs and Day 3 engine results: INFO/toyesh_session_handoff_2026-05-12.txt

WHAT TOYESH BUILT TODAY:
- Financial ratios: compute_ratios(dataset) returns DSO 36.2 days, DPO 93.1 days,
  working capital €26,483.04, open AR €91,304.61, open AP €64,821.57
- DataReadinessReport.ratios is now always populated (FinancialRatios | None in model,
  never None in practice when loaded via ReadinessEngine.run())
- CIT check now also warns if gap > €5,000 absolute (in addition to the % threshold)
- load_all_from_exact(access_token, division_id, period_start, period_end) in data_loader.py
  — full Exact Online API scaffold, all field mappings documented, pagination handled
- 16 passing integration tests (was 10); working capital pinned to €26,483.04
- Pre-commit hook added locally blocking data file staging
- CLAUDE.md and README updated


YOUR CODE IS CORRECT / NEEDS FIXING:
-------------------------------------
CRITICAL BUG 1 — Wrong model ID in backend_FastAPI_emma/services/reasoning.py line 76:
  WRONG:   model="claude-sonnet-4-20250514"
  CORRECT: model="claude-sonnet-4-6"
  This throws an Anthropic API error on every Claude call. Fix this first.

CRITICAL BUG 2 — Data leak on your branch:
  Run: git rm -r --cached "00 Dataroom hackathon/" && git commit -m "untrack client data files"
  (39 files from the dataroom are tracked on origin/emma — financial client data must not be in git)

CRITICAL BUG 3 — Engine not on your branch (your app crashes on startup):
  Your analyze.py imports backend.services.readiness_engine which only exists on main.
  Fix: git fetch origin && git merge origin/main


CRITICAL: DO THESE IN ORDER BEFORE TOUCHING ANY CODE
------------------------------------------------------
1. git fetch origin && git merge origin/main
   (Take Toyesh's version if CLAUDE.md conflicts)
2. Fix model ID in reasoning.py line 76 (see above)
3. git rm -r --cached "00 Dataroom hackathon/" && git commit -m "untrack client data files"
Then verify: uvicorn backend_FastAPI_emma.main:app --reload
             curl http://localhost:8000/health  → should return 200


CHECK IDs / API CONTRACTS — CHANGED THIS SESSION:
--------------------------------------------------
DataReadinessReport has a new field: ratios: FinancialRatios | None
  - Populated automatically when engine runs (always present after merge)
  - Your frontend accesses it as: result.readiness.ratios
  - FinancialRatios fields (all are RatioResult with .value, .reliable, .note):
      dso_days         — Days Sales Outstanding (36.2 on this dataset)
      dpo_days         — Days Payable Outstanding (93.1 on this dataset)
      working_capital  — open AR minus open AP (€26,483.04 on this dataset)
      revenue_period   — total revenue in period (€920,966.91)
      purchases_period — total purchases in period (€254,059.08)
      open_ar          — unmatched sales invoices (€91,304.61)
      open_ap          — unmatched purchase invoices (€64,821.57)
  - No change needed to your AnalysisResult schema — ratios flow through readiness.ratios
  - reliable=False means the ratio is computable but data quality issues make it unreliable;
    show the .note field to the user in that case

UX CHANGE — advice_ready=False no longer means hard block:
  Old: return bare blocked_reason string
  New: call call_claude_guided(report) → returns JSON with guided fix list
  See code snippet [1] below


GIT:
----
git fetch origin && git merge origin/main
Then verify tests: pytest tests/test_integration.py -v  (should show 16 passed)


YOUR TASKS TODAY (Day 5 — final day):
--------------------------------------
PRIORITY 1 — MUST DO (demo breaks without these):
  a) Fix 3 critical bugs above (merge, model ID, data leak)
  b) Wire guided-diagnosis Claude call — see snippet [1] below
  c) Add ratios display panel to frontend:
     - DSO, DPO, working capital as key metrics cards
     - Show .note if .reliable is False
     - Access: result.readiness.ratios.dso_days.value
  d) Add 3 missing frontend check cards:
     - cit_preliminary_deviation  (medium severity)
     - vat_provisional_correction (medium severity)
     - ap_aging_stale             (medium severity)
     (These check_ids exist in engine since Day 3 but have no frontend cards yet)

PRIORITY 2 — HIGH VALUE FOR DEMO:
  e) Add market comparison to Claude system prompt — snippet [3] below
  f) Add LangWatch tracing — snippet [2] below (3 lines, shows guardrail working in live trace)

PRIORITY 3 — AFTER EXACT ONLINE CREDENTIALS ARRIVE (coordinate with Toyesh):
  g) Add auth endpoints to FastAPI: GET /auth/exact/redirect, GET /auth/exact/callback
  h) Add use_exact_online toggle to analyze endpoint — snippet [4] below


--- CODE SNIPPETS (copy-paste ready) ---

[1] GUIDED DIAGNOSIS — add to reasoning.py alongside call_claude():
--------------------------------------------------------------------
import json
from backend.models import DataReadinessReport

def call_claude_guided(report: DataReadinessReport) -> str:
    """Called when advice_ready=False. Returns JSON guidance on what to fix."""
    issues = [
        {"check": c.check_id, "status": c.status, "description": c.description,
         "amount": c.affected_amount}
        for c in report.checks if c.status in ("fail", "blocker", "warn")
    ]
    prompt = f"""You are a financial data quality advisor.
The readiness engine found these issues preventing a closing advisory:

{json.dumps(issues, indent=2)}

For each issue: (1) explain in plain English what is wrong, (2) why it matters for closing,
(3) the exact step to fix it. Be direct. Prioritise blockers first.
Return JSON: {{"guidance": [{{"issue": str, "impact": str, "fix_step": str}}]}}"""

    resp = client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=1024,
        messages=[{"role": "user", "content": prompt}],
    )
    return resp.content[0].text

Add to AnalysisResult in schemas.py:
    guided_response: str | None = None  # populated when advice_ready=False

In analyze.py, replace the bare blocked_reason path with:
    if not report.advice_ready:
        guidance = call_claude_guided(report)
        return AnalysisResult(
            readiness=report,
            advisory_outputs=[],
            blocked_reason="Data issues found — see guided_response",
            guided_response=guidance,
        )


[2] LANGWATCH TRACING — 3 additions to reasoning.py:
------------------------------------------------------
# After "import anthropic":
import langwatch
langwatch.setup()  # reads LANGWATCH_API_KEY from env — add to .env.example

# Decorate both claude functions:
@langwatch.trace()
def call_claude(report: DataReadinessReport) -> list[AdvisoryOutput]:
    ...  # existing body unchanged

@langwatch.trace()
def call_claude_guided(report: DataReadinessReport) -> str:
    ...  # existing body unchanged

# Add to requirements.txt: langwatch
# Add to .env.example: LANGWATCH_API_KEY=your_key_here
# Note: verify exact API at https://github.com/langwatch/langwatch


[3] MARKET COMPARISON — add this paragraph at END of existing system prompt in reasoning.py:
---------------------------------------------------------------------------------------------
"""After presenting the findings, add a section comparing the company's financial
ratios to typical Dutch SME benchmarks for a bicycle workshop of similar revenue scale
(~€920K/year). Use your training knowledge about Dutch SME financial benchmarks.
Label this section clearly: 'Industry context (AI-estimated — for indicative purposes only).'"""


[4] EXACT ONLINE LOADER SWITCH — add to analyze.py once credentials arrive:
-----------------------------------------------------------------------------
# Add to imports at top:
from backend.services.data_loader import load_all, load_all_from_exact

# In the analyze endpoint (coordinate with Toyesh on exact token storage):
# exact_token = state.get("exact_access_token")  # from OAuth callback
# division_id = state.get("exact_division_id")
if use_exact_online and exact_token:
    dataset = await load_all_from_exact(exact_token, division_id, period_start, period_end)
else:
    dataset = await load_all(DATA_FOLDER, period_start, period_end)


CONTEXT FOR YOUR CLAUDE CODE SESSION:
--------------------------------------
Tell your Claude Code: "I'm Emma on the Consult&Co hackathon (Day 5, final day).
Read toyesh_session_handoff_2026-05-13.txt first. Start by merging main, fixing
model ID in reasoning.py line 76 (claude-sonnet-4-20250514 → claude-sonnet-4-6),
and removing data files from git (git rm -r --cached '00 Dataroom hackathon/').
Then wire the guided-diagnosis Claude call (snippet [1]), expose ratios in the
frontend (result.readiness.ratios), and add 3 missing check cards:
cit_preliminary_deviation, vat_provisional_correction, ap_aging_stale."
