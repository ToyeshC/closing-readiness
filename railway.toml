[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn backend_FastAPI_emma.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
