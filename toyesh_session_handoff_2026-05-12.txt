SESSION HANDOFF — Toyesh to Emma
Date: 12 May 2026 (Day 2)
Branch: toyesh (being pushed to main — pull before you start)

════════════════════════════════════════════════════════════
HEADLINE: The harness engine is done. Your API routes will work
once you pull main.
════════════════════════════════════════════════════════════


── WHAT TOYESH BUILT TODAY ──────────────────────────────────

backend/services/normalizer.py       Converts raw dicts → SourceLine objects
backend/services/readiness_engine.py  ReadinessEngine(dataset).run() → DataReadinessReport
backend/services/checks/suspense.py  Account 1250 — BLOCKER
backend/services/checks/todo_discrepancy.py
backend/services/checks/revenue_reconciliation.py
backend/services/checks/capex_opex.py
backend/services/checks/bank_coverage.py
backend/services/checks/ar_aging.py
backend/services/checks/timing_differences.py
backend/services/checks/vat_reconciliation.py
README.md                            Project overview + how to run

Your analyze.py already calls these correctly. Just pull main.


── LIVE ENGINE OUTPUT AGAINST REAL DATA ────────────────────

Score: 40% | Advice ready: False

[BLOCKER] Suspense account balance      €39,893.52  (8 source lines)
[FAIL   ] Unimported file discrepancies            (1,239 records pending)
[FAIL   ] Revenue reconciliation        €920,224.41 (GL has no 2024 revenue entries yet)
[PASS   ] CapEx/OpEx misclassification
[WARN   ] Bank statement coverage                  (85% business day coverage)
[FAIL   ] Accounts receivable aging     €39,703.64  (14 overdue invoices)
[PASS   ] Timing differences
[PASS   ] VAT reconciliation                       (quarterly totals matched from PDF)

advice_ready=False because of suspense blocker → your blocked_reason path fires correctly.


── YOUR CODE IS CORRECT ─────────────────────────────────────

analyze.py:    advice_ready gate is right — early return with blocked_reason, Claude never called ✓
analyze.py:    period_start / period_end defaults (2024-01-01 / 2024-12-31) work fine ✓
schemas.py:    AnalysisResult, AdvisoryOutput shapes match what the engine returns ✓


── ONE FIX NEEDED IN YOUR CODE ──────────────────────────────

reasoning.py line: model="claude-sonnet-4-20250514"
That model ID follows the old Claude 3 naming convention and will fail.

Change to: model="claude-sonnet-4-6"

That's the current Claude Sonnet 4. Double-check in your Claude Code session
by asking: "what is the correct model ID for Claude Sonnet 4?"


── CRITICAL: CHECK IDs — 8 CHECKS, NOT 9 ───────────────────

Your partner_knowledge_base.md and emma_spec.md list "draft_entries" as a check_id.
That check was DROPPED on Day 1 — there is no status column in the export files.

The replacement is "todo_discrepancy" — already in your list.

Remove "draft_entries" from your frontend wherever you reference it.
The 8 real check_ids are:
  suspense_account_balance
  todo_discrepancy
  revenue_reconciliation
  capex_opex_misclassification
  bank_statement_coverage
  ar_aging_stale
  timing_differences
  vat_reconciliation


── URGENT: DATA LEAK ON YOUR BRANCH ─────────────────────────

Your .gitignore has "00 Dataroom hackathon/" but the files are already tracked
by git (they were committed before the .gitignore entry). .gitignore has no
effect on already-tracked files.

Run these commands NOW before pushing anything:

  git rm -r --cached "00 Dataroom hackathon/"
  git rm --cached .DS_Store
  echo "**/.DS_Store" >> .gitignore
  git add .gitignore
  git commit -m "untrack data folder and DS_Store from git"

This removes the files from git history going forward (existing history still
has them — don't share the repo externally until after the hackathon).


── CLAUDE.md CONFLICT ON MERGE ──────────────────────────────

You created a CLAUDE.md on your branch. Toyesh also created one on main.
When you merge emma → main, there will be a conflict on this file.

Resolution: take Toyesh's version. His CLAUDE.md has the Dutch column name
reference table, check_ids, branch rules, and loading quirks all in one place.
Your CLAUDE.md is a subset of his.


── REPO STATE AFTER TOYESH PUSHES TO MAIN ───────────────────

Pull main before starting:
  git fetch origin
  git merge origin/main

New files you'll get:
  backend/services/normalizer.py
  backend/services/readiness_engine.py
  backend/services/checks/ (8 modules)
  README.md
  CLAUDE.md (updated with check_ids table)
  .gitignore (updated with .DS_Store rule)


── YOUR DAY 2 TASKS ─────────────────────────────────────────

1. Fix model ID in reasoning.py (see above)
2. Untrack data files (git rm -r --cached — see above)
3. Pull main
4. Run: uvicorn backend_FastAPI_emma.main:app --reload
5. Test POST /api/v1/readiness → should return 40% score, advice_ready=false
6. Build the 3 frontend screens (see emma_spec.md for full spec):
     Screen 1: Connect / trigger readiness check
     Screen 2: Report grid — 8 check cards with status icons + "Show source" button
     Screen 3: Advisory outputs — only reachable if advice_ready=true
7. Wire "Show source" to GET /api/v1/readiness/{check_id}/sources
   (This is the judge's key moment — show raw Dutch GL rows)

For Screen 2, the source lines on the suspense check have 8 GL entries
with account 1250 and amounts — that's what the judges need to see.


── CONTEXT FOR YOUR CLAUDE CODE SESSION ─────────────────────

Your session can read CLAUDE.md (in project root) for all project context.
Dutch column names, branch rules, team split, and key invariants are all there.

To start your session you can just say:
"Day 2 — build the 3 frontend screens. Engine is live. See CLAUDE.md."
